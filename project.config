[access]
	inheritFrom = All-Projects
[access "refs/*"]
	owner = group user/libao (np-libao)
[access "refs/for/refs/*"]
	read = group user/libao (np-libao)
	push = group user/libao (np-libao)
	label-Code-Review = -2..+2 group user/libao (np-libao)
	label-Verified = -1..+1 group user/libao (np-libao)
	rebase = group user/libao (np-libao)
	removeReviewer = group user/libao (np-libao)
	submit = group user/libao (np-libao)
[access "refs/heads/*"]
	read = group user/libao (np-libao)
	create = group user/libao (np-libao)
	push = +force group user/libao (np-libao)
	pushMerge = group user/libao (np-libao)
[access "refs/tags/*"]
	push = +force group user/libao (np-libao)
	pushTag = group user/libao (np-libao)
	pushSignedTag = group user/libao (np-libao)
